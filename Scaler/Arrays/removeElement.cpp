#include <bits/stdc++.h>
using namespace std;

int main()
{
    system("cls");
    vector<int> nums = {1, 2, 3, 3, 3, 4, 3, 5, 3, 3, 3};
    int val = 2;
    int n = nums.size();
    // int pos = removeAllOcc(nums);
    // for(int i=0;i<n- pos)
    // auto it = remove(nums.begin(), nums.end(), val);
    // cout << n << it - nums.begin() << endl;
    // for (int i = 0; i < n; i++)
    // {
    //     if (arr[i] != target)
    //     {
    //         arr[i - cnt] = arr[i];
    //     }
    //     else
    //     {
    //         cnt++;
    //     }
    // }
    auto it = nums.begin() + 2;
    return 0;
}